@startuml
!pragma teoz true
title Robustness - UC5: Manage Volunteers
hide footbox

skinparam shadowing false
skinparam roundcorner 12
skinparam dpi 200
skinparam maxMessageSize 30
skinparam sequenceMessageAlign center
skinparam responseMessageBelowArrow true
skinparam ParticipantPadding 18

actor "Branch Manager" as Manager

box "NGO Management System"
boundary "Volunteer\nPortal" as UI
control "Workforce\nManager" as WorkMgr
entity "Session\nStore" as Sess
database "User\nDB" as UserDB
end box

Manager -> UI : addVolunteer(name, phone)
UI -> WorkMgr : registerVolunteer(data)

WorkMgr -> Sess : getManagerBranchID()
Sess --> WorkMgr : return(branchID)

WorkMgr -> UserDB : insertUser(role='Volunteer')
WorkMgr -> UserDB : mapToBranch(userID, branchID)

WorkMgr --> UI : result(success)
UI --> Manager : updateRosterView()
@enduml